Implemente uma função tabuada que receba como parâmetro um número inteiro e escreva a tabuada
desse número.

def tabuada(numero):
    for i in range(1, 11):
        print("{} x {} = {}".format(numero, i, numero * i))

# Pedir ao usuário para fornecer o número para a tabuada
numero = int(input("Digite um número para ver sua tabuada: "))

# Chamar a função tabuada com o número fornecido pelo usuário
tabuada(numero)
